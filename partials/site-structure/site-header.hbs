<header id="it-head" class="it-head">
    <nav class="it-head-inner it-container">
        <div class="it-head-brand">
            <h1>
                <a class="it-head-logo" href="{{@site.url}}">
                    {{#if @site.logo}}
                        <img src="{{@site.logo}}" alt="{{@site.title}}" />
                    {{else}}
                        {{@site.title}}
                    {{/if}}
                </a>
            </h1>
            <a class="it-burger" role="button">
                <div class="it-burger-box">
                    <div class="it-burger-inner"></div>
                </div>
            </a>
        </div>
        <div class="it-head-menu">
            {{navigation}}
        </div>
        <div class="it-head-actions">
            <div class="it-head-actions-list">
                {{#if @labs.members}}
                	{{#if @member}}
                		<a class="account-link" href="{{@site.url}}/account/" title="{{t "Account"}}" >{{t "Account"}}</a>
                		<a class="sign-out" href="#signout" data-members-signout>{{t "Sign out"}}</a>
                	{{else}}
                		<a class="signin-link" href="{{@site.url}}/signin/">{{t "Signin"}}</a>
                	    <a class="signup-link" href="{{@site.url}}/signup/">{{t "Signup"}}</a>
                	{{/if}}
                {{/if}}
                <a class="search-link" href="#searchinghost-easy" title="{{t "Search"}}" aria-label="Search">{{> "icons/search"}}</a>
            </div>
        </div>
    </nav>
</header>

